apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-http-kuard
spec:
  podSelector:
    matchLabels:
      app: kuard
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - ipBlock:
            cidr: 10.98.151.0/24
      ports:
        - protocol: TCP
          port: 8080
  egress:
  - to:
    - ipBlock:
        cidr: 10.98.151.0/24
    ports:
    - protocol: TCP
      port: 8080
